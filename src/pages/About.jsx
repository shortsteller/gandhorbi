import React from 'react';

export const About = () => {
  return (
    <div className="fade-in" style={{ minHeight: '60vh', backgroundColor: 'var(--bg-warm-linen)' }}>
      {/* Completely blank for now as requested */}
    </div>
  );
};
